# 기획서 — 프랙탈 에이전트 피라미드 + 의미 게이트 (2026-06-11)

# L0: 계층마다 판단 기준이 달라지면 작업이 방향을 잃고 재작업과 위계 붕괴가 발생한다 — 이를 모든 스케일에서 방지
# L1: 기존 4계층(지식·측정·학습·그래프)에 새 레이어를 만들지 않고 "에이전트 차원"과 "의미 차원"을 심화로 추가

> Leader(Opus 전략 에이전트, agentId a071314d95b645710) 설계 보고 기반.
> 외부 실증: Anthropic multi-agent research system(+90.2% 오케스트레이터-워커),
> Claude Agent SDK subagents, 하이브리드 게이트(결정적+LLM-judge), $47K 무한루프 사례(예산 알람≠강제).

## 닫아야 할 결함 4개

| # | 결함 | 해소 |
|---|------|------|
| 1 | SUB AGENT/SUB GOAL 개념 빈약 — 복잡 작업이 단일 컨텍스트에서 L0+L3 동시 처리 | goal_registry + 분해 게이트: Leader/Manager 호출에 서브골·수용기준 강제 |
| 2 | 의미 매칭 부재 — 모든 강제가 regex, 의미 판단은 "강제 불가"로 방치 | 2단 게이트: 결정적층(즉시 차단) + LLM-judge층(독립 Agent 채점자, 씨앗 4 판단자 독립성) |
| 3 | 감시 사각 — 스킬·에이전트·서브골 추적 없음 | Stop/SubagentStop 텔레메트리: 트랜스크립트에서 Agent tool_use·parent_tool_use_id·완료여부 적재 |
| 4 | 프랙탈 불완전 — 목표·에이전트 계층에 위계 없음 | 목표·에이전트를 그래프 노드화, parent_tool_use_id = 위계 에지 → 폐쇄 질의 확장 |

## L2 — Worker 분해 (의존 순서)

- W1 서브골 위계 게이트 (독립) — goal_decompose_gate + 서브골 형식 강제
- W2 에이전트 텔레메트리 (독립) — agent_telemetry 훅 + 측정 파일
- W3 LLM-judge 의미 게이트 (W2 의존)
- W4 루프/데드엔드 강제 (W2 의존) — 관측이 아니라 하드 차단
- W5 그래프 폐쇄 확장 (W1 의존) — 목표·에이전트 노드 편입
- W6 문서 SSOT 동기화 (최후)

## L3 — 수용 기준

각 Worker 산출물은 격리 픽스처 테스트(차단 케이스 exit 2 / 통과 케이스 exit 0) 실측 출력과 함께만 완료 인정.
전 훅은 ~/.claude/hooks 배포 + 레포 미러 + settings.json 배선 + 커밋·푸시까지가 완료 정의.

## 감시 체계 자기 적용

이 기획 자체가 새 측정 대상이다: 에이전트 텔레메트리가 가동되면 이 세션의 Leader/Worker 호출이
첫 데이터가 된다 (hook-self-application 원칙 — 새 메커니즘의 최초 테스트 케이스는 나 자신).
